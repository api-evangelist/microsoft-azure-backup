naftiko: 1.0.0-alpha2
info:
  label: Azure Backup REST API — Operations
  description: 'Azure Backup REST API — Operations. 1 operations. Lead operation: Azure Backup REST API List operations. Self-contained
    Naftiko capability covering one Microsoft Azure Backup business surface.'
  tags:
  - Microsoft Azure Backup
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_BACKUP_API_KEY: MICROSOFT_AZURE_BACKUP_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-backup-operations
    baseUri: https://management.azure.com
    description: Azure Backup REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.RecoveryServices-operations
      path: /providers/Microsoft.RecoveryServices/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Backup REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_BACKUP_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-backup-operations-rest
    port: 8080
    description: REST adapter for Azure Backup REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-recoveryservices/operations
      name: providers-microsoft-recoveryservices-operations
      description: REST surface for providers-Microsoft.RecoveryServices-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Backup REST API List operations
        call: microsoft-azure-backup-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-backup-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Backup REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-backup-rest-api-list
      description: Azure Backup REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-backup-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
